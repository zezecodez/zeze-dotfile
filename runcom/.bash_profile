#!bin/sh

# Add `~/bin` to the `$PATH`
export PATH="$HOME/bin:$PATH";

# Do I need commented stuff below?
#for DOTFILE in `find /Users/zezemanolo/Desktop/learnersguild/week18/zedotfiles/`
#need .dotfiles at the end of the path above?
